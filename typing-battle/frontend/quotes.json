[
  { "text": "const fetchData = async (url) => { const res = await fetch(url); return await res.json(); };", "source": "JavaScript Async" },
  { "text": "function debounce(fn, ms) { let timer; return (...args) => { clearTimeout(timer); timer = setTimeout(() => fn(...args), ms); }; }", "source": "JavaScript Utilities" },
  { "text": "const users = data.filter(u => u.active).map(u => ({ id: u.id, score: u.mmr * 1.5 }));", "source": "JavaScript Functional" },
  { "text": "SELECT u.id, u.username, COUNT(m.id) as total_wins FROM users u JOIN matches m ON u.id = m.winner_id GROUP BY u.id ORDER BY total_wins DESC LIMIT 10;", "source": "SQL Aggregation" },
  { "text": "def binary_search(arr, target): left, right = 0, len(arr) - 1; while left <= right: mid = (left + right) // 2; return mid", "source": "Python Algorithms" },
  { "text": "import { useState, useEffect } from 'react'; export default function App() { const [count, setCount] = useState(0); return count; }", "source": "React Hooks" },
  { "text": "const server = http.createServer((req, res) => { res.writeHead(200, { 'Content-Type': 'application/json' }); res.end(data); });", "source": "Node.js Server" },
  { "text": "fn calculate_hash<T: Hash>(t: &T) -> u64 { let mut s = DefaultHasher::new(); t.hash(&mut s); s.finish() }", "source": "Rust Syntax" },
  { "text": "int partition(vector<int>& arr, int low, int high) { int pivot = arr[high]; int i = low - 1; for (int j = low; j < high; j++) { } }", "source": "C++ Algorithms" },
  { "text": "const token = jwt.sign({ sub: user.id, role: 'admin' }, process.env.JWT_SECRET, { expiresIn: '7d' });", "source": "Node.js Auth" },
  { "text": "docker run -d --name redis-cache -p 6379:6379 -v redis-data:/data redis:7-alpine --appendonly yes", "source": "Docker DevOps" },
  { "text": "const [state, dispatch] = useReducer((prev, action) => ({ ...prev, [action.type]: action.payload }), initialState);", "source": "React Reducer" },
  { "text": "git commit -m 'refactor: decouple matchmaking queue from websocket session state' && git push origin main", "source": "Git CLI" },
  { "text": "try { const doc = await db.collection('metrics').findOneAndUpdate({ id }, { $inc: { views: 1 } }); } catch (err) { }", "source": "MongoDB Query" },
  { "text": "CREATE TABLE matches (id TEXT PRIMARY KEY, p1_id TEXT NOT NULL, p2_id TEXT NOT NULL, winner_id TEXT, mmr_delta INT DEFAULT 25);", "source": "SQLite DDL" },
  { "text": "const memoized = useMemo(() => heavyComputation(items, threshold), [items, threshold]);", "source": "React Performance" },
  { "text": "export interface MatchSession { id: string; p1: WebSocket; p2: WebSocket; isRanked: boolean; startTime: number; }", "source": "TypeScript Types" },
  { "text": "kubectl apply -f deployment.yaml && kubectl rollout status deployment/syntax-rush-backend -n production", "source": "Kubernetes DevOps" },
  { "text": "Any fool can write code that a computer can understand Good programmers write code that humans can understand", "source": "Martin Fowler" },
  { "text": "First solve the problem Then write the code If you spend too long on the second step you skipped the first", "source": "John Johnson" },
  { "text": "Talk is cheap Show me the code Make sure that code is clean fast and does exactly what it needs to do", "source": "Linus Torvalds" },
  { "text": "There are only two hard things in computer science cache invalidation naming things and off by one errors", "source": "Phil Karlton" }
]
